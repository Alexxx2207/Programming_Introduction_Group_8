
## Какво ще изведе кода

```cpp
#include<iostream>

int main() {
    int x = 10;

    if(x += x -= 10) {
        std::cout << (x *= 20);
    } else {
        std::cout << x;
    }
    
	return 0;
}
```


```cpp
#include<iostream>

int main() {
    int x = 10;

    std::cout << ((x == 10) || (x / 0));

	return 0;
}
```

```cpp
#include<iostream>

int main() {
    int x = 10;

    std::cout << ((x == 20) || (x / 0));

	return 0;
}
```

```cpp
#include <iostream>
int main() {
    int i = 1;

    std::cout << (i + ++i) << "\n";
}
```

```cpp
#include <iostream>

int main() {
    int i = 1;

    std::cout << (i + i++) << "\n";
}
```

```cpp
#include <iostream>
int main() {
    char c = -1;
    
    unsigned int u = c;

    std::cout << (u > 0) << "\n";
}
```

```cpp
#include<iostream>

int main() {
    int a = 5;
    
    int res = (a += 10) + (a += 1);

    std::cout << res;

	return 0;
}
```

## Задачи по материала 

### Задача 1

Да се напише BMI калкулатор: програма, която приема две числа - тегло в кг и височина в сантиметри и пресмята формулата: $BMI = \frac{weight}{height^2}$, където height е в метри.

### Задача 2

Да се напише програма, която приема число(темература в Келвини `K`) и го представя в `°C` и `°F`.
<br />
$°C = K - 273.15$
<br />
$°F = (K - 273.15$) × 1.8 + 32$

### Задача 3

Напишете програма, която разменя стойностите на две числови променливи.
<br />
**Бонус: Използвайте само аритметични оператори.** 

### Задача 4

Направете калкулатор, който пресмята корените на квадратно уравнение(ако има такива) по подадени от клавиатурата коефициенти `a`, `b`, `c` на уравнението $ax^2 + bx + c = 0$. Ако няма, изпечатайте дискриминантата. 

### Задача 5

Направете програма, която приема цяло число и връща `1`-ца, ако е четно, и `0`, ако е **НЕ**четно. 
<br />
**Не ползвайте условни оператори**.

### Задача 6

Напишете програма, която приема 4 числа и проверява кое от последните 3 числа е най-близо до първото.

### Задача 7

При получени първо трицифрено число и после цифра, изпечатайте `1`, ако цифрата се съдържа в числото. Иначе `0`, ако го няма.

### Задача 8

Да се напише програма, която по зададено време (часове и минути) намира ъгъла между стрелките на аналогов часовник, показващ това време. Ъгъл между две стрелки ще наричаме в случая по-малкият от двата ъгъла, които се образуват. 

### Задача 9

По подадени 3 точки, изпечатайте `1`, ако те се намират на една линия. Иначе, `0`.

### Задача 10

По подадени 4 точки, изпечатайте `1`, ако 4-тата точка се намира в тригъгълника, определен от първите 3 точки. Иначе, `0`.

### Задача 11

По подадени `a`, `b` и `n` да се намери сумата на първите n числа >= `a`, които се намират на "разстояние" `b` едно от друго, започвайки от `a`.


## Извън преподадения материал / на когото му е скучно

### Задача 1

**_За тази задача НЕ може да ползвате допълнителни библиотеки, примерно: `<cstring>` `<string>` и `<regex>`. Напишете си всичко сами .Трябва да ползвате `char*` - тоест динамично заделяне и освобождаване на памет с `new` и `delete`._**

Получавате от стандартния вход първо дължина на низ `str`, после низа `str` и накрая низ-шаблон `pattern` с максимална дължина 1000 ASCII символа, като `@` и `#` имат специално значение:
- `#` означава точно един произволен символ
- `@` означава нула или повече произволни символа

Проверете дали `str` пасва на `pattern`.

Пояснение: `str` **не** съдържа `@` и `#`.

Примери:
```
Input: str = "xx", pattern = "x"
Output: false
```

```
Input: str = "gfjka", pattern = "gf##a"
Output: true
```

```
Input: str = "abfa", pattern = "@a"
Output: true
```

### Задача 2

По подадено число `n`, намерете **броя** на начините да наредите `n` кралици от шаха на `n x n` поле така, че никои две от тях не се атакуват помежду си.